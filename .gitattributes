# Check out every text file with LF on all platforms. Golden conformance
# files are compared byte-for-byte, and Hirð sources, snapshots, and emitted
# Erlang are all written assuming LF.
* text=auto eol=lf
